// Bump this string on every push to main so it's immediately, visually
// obvious on the TV/browser which build is actually running — no need to
// check GitHub or guess whether a redeploy landed.
export const BUILD_LABEL = "Merge 8";
